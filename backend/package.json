{
  "name": "bv-squares-backend",
  "version": "1.0.0",
  "description": "BV Squares backend Lambda functions",
  "main": "src/handler.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/",
    "deploy": "sam build && sam deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0",
    "uuid": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}